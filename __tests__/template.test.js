// import template from './../src/js/template.js';

describe('template', () => {
  let template;

  beforeEach(() => {
    template = new Template();
  });

  test('', () => {    
    expect(tempObj.value).toEqual(null);
  });

});